{
  "name": "@repo/tsconfig",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "echo 'TypeScript config requires no build step'",
    "typecheck": "echo 'TypeScript config requires no typecheck'"
  }
}
