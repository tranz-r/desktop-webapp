# Tranzr Web App (apps/web)

## Agent OS

Agent OS docs live at `.agent-os/instructions/core/`. Start with `analyze-product.md`.

If the CLI is available in your registry, you can run:
```bash
pnpm agent-os --help
```
